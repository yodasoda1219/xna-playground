{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "make",
            "args": [
                "-C",
                "${workspaceFolder}"
            ],
            "type": "process",
            "problemMatcher": "$msCompile"
        }
    ]
}